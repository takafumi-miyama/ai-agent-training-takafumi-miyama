# Python 3.10+ required

# UI / API（全Day共通 — 共通画面・Runner API）
streamlit>=1.31,<2
fastapi>=0.110,<1
uvicorn>=0.27,<1
httpx>=0.27,<1

# AWS（Day02以降で使用）
boto3>=1.34,<2

# LangChain / LangGraph（Day04以降で使用）
langchain>=0.3,<0.4
langchain-aws>=0.2,<0.3
langgraph>=0.2,<0.3

# Utilities
python-dotenv>=1.0,<2
