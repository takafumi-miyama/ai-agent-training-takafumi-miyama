# Day09 suite.json について

`day09/suite.json` は、Day09の動作確認用のサンプル入力セットです。

- 目的：
  - `day09/app.py` の `load_suite()` / `run_case()` を実装したあとに、まずはこのファイルで流れが通ることを確認する
- 注意：
  - `case2-boundary-long` は境界テストの雛形です（必要に応じて長文に置き換えてください）
  - `case5-rag-hit-miss` はDay05等のRAG実装がないと意味が薄いので、適宜差し替えOKです
